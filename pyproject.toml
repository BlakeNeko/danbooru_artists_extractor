[project]
name = "danbooru artists extractor"
version = "0.1.0"
description = "Extract and clean Danbooru artist tags into a CSV file"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
]
